FROM node:18-alpine

WORKDIR /app

# Install dependencies (production only for lighter build)
COPY package*.json ./
RUN npm install && npm cache clean --force

# Copy frontend code
COPY . .

EXPOSE 3000
CMD ["npm", "run", "dev"]
